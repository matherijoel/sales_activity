$(document).ready(function() {
    // Initialize a datepicker for fields with the 'datepicker' class
    $(".datepicker").datepicker({
        dateFormat: 'yy-mm-dd' // Format the date as YYYY-MM-DD
    });
});
